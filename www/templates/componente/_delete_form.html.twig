<form method="post" action="{{ path('app_componente_delete', {'id': componente.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ componente.id) }}">
    <button class="btn">Delete</button>
</form>
