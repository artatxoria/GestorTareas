<nav class="bg-blue-600 fixed top-0 left-0 w-full z-50 shadow-md">
    <div class="container mx-auto px-4 flex items-center justify-between h-16">
        
        {# Logo + Marca #}
        <div class="flex items-center gap-3">
            <img src="{{ asset('imagenes/BideDigitala_logo.png') }}" alt="Logo Gestor" class="w-10 h-auto">
            <a href="{{ path('home') }}" class="text-white text-xl font-bold">GestorTareas</a>
        </div>

        {# Botón hamburguesa (móvil) #}
        <button 
            class="text-white lg:hidden focus:outline-none"
            onclick="document.getElementById('navbarNav').classList.toggle('hidden')"
        >
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                      d="M4 6h16M4 12h16M4 18h16"/>
            </svg>
        </button>

        {# Menú #}
        <div id="navbarNav" class="hidden lg:flex lg:items-center">
            <ul class="flex flex-col lg:flex-row lg:gap-2 bg-blue-600 lg:bg-transparent w-full lg:w-auto absolute lg:static top-16 left-0 px-4 pb-4 lg:p-0">
                <li><a href="{{ path('home') }}"     class="block px-3 py-2 text-white hover:bg-blue-500 rounded transition">Inicio</a></li>
                <li><a href="{{ path('about') }}"    class="block px-3 py-2 text-white hover:bg-blue-500 rounded transition">Sobre mí</a></li>
                <li><a href="{{ path('services') }}" class="block px-3 py-2 text-white hover:bg-blue-500 rounded transition">Servicios</a></li>
                <li><a href="{{ path('contact') }}"  class="block px-3 py-2 text-white hover:bg-blue-500 rounded transition">Contacto</a></li>
            </ul>
        </div>

    </div>
</nav>