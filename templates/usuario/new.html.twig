{# templates/usuario/new.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Crear Nuevo Usuario{% endblock %}

{% block body %}
    <div class="container mx-auto mt-8">
        <h1 class="text-3xl font-bold mb-6 text-gray-800">Crear Nuevo Usuario</h1>

        {# Incluimos el fragmento del formulario y le pasamos una etiqueta para el botón #}
        {{ include('usuario/_form.html.twig', {'button_label': 'Guardar Usuario'}) }}

        <div class="mt-4">
            <a href="{{ path('listado_usuarios') }}" class="text-blue-500 hover:underline">
                ← Volver al listado
            </a>
        </div>
    </div>
{% endblock %}
